[build]
    publish = "public"
    command = "make serve-production"

    [build.environment]
        HUGO_VERSION = "0.122.0"

[context.production.environment]
  HUGO_ENV           = "production"
  HUGO_ENABLEGITINFO = "true"

[context.deploy-preview]
  command = "make serve-production"
